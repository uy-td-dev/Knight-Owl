+++
schema_version = 1
id          = "audit"
name        = "audit"
description = "Quick security/quality audit of staged changes"

[expansion]
type     = "workflow"
workflow = "audit_security"
+++

# /audit

Quick security/quality audit of staged changes.
